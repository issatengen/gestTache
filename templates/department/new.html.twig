{% extends 'base.html.twig' %}

{% block title %}New Department{% endblock %}

{% block body %}
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800 text-primary-emphasis">Ajouter un département</h1>
        <a href="{{ path('app_department_index') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
            <i class="fa fa-arrow-left me-1"></i> Liste des départements
        </a>
    </div>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}" class="text-decoration-none">Tableau de bord</a></li>
            <li class="breadcrumb-item active" aria-current="page">Liste des départements</li>
        </ol>
    </nav>
    <hr class="sidebar-divider mb-4">

    <div class="container mt-5">
        <div class="card">
            <hr class="my-1">
            <div class="row container mt-2">
                <div class="col-md-8 bg-light p-4 rounded shadow">
                    {{ include('department/_form.html.twig') }}
                </div>
                <div class="col-md-4 text-center mt-2">
                    <i class="fa-solid fa-building fa-3x text-primary-emphasis"></i>
                    <h2 class="mt-3">Pourquoi ajouter un département&nbsp;?</h2>
                    <p class="lead">Ajouter un département permet de structurer les compétences et d'organiser efficacement les ressources humaines dans l'application.</p>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
