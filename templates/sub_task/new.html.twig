{% extends 'base.html.twig' %}

{% block title %}New SubTask{% endblock %}

{% block body %}
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Ajouter une sous-tâche</h1>
        <a href="{{ path('app_task_index') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
            <i class="fa fa-arrow-left me-1"></i> Tâches
        </a>
    </div>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}" class="text-decoration-none">Tableau de bord</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_task_index') }}" class="text-decoration-none">Tâches</a></li>
            <li class="breadcrumb-item active" aria-current="page">Ajouter sous-tâche</li>
        </ol>
    </nav>
    <hr class="sidebar-divider mb-4">
    
    <div class="container mt-5">
        <div class="card">
            <hr class="my-1">
            <div class="row container mt-2">
                <div class="col-md-6 bg-light p-4 rounded shadow">
                    {{ form_start(form) }}
                    <div>
                        <label for="libelle">Libellé de l'activité </label>
                        {{ form_widget(form.label, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div>
                        <label for="debut">Date début </label>
                        {{ form_widget(form.debut, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div>
                        <label for="fin">Date fin </label>
                        {{ form_widget(form.fin, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div>
                        <label for="task">Tâche principale</label>
                        {{ form_widget(form.task, {'attr': {'class': 'form-select'}}) }}
                    </div>
                    <button class="btn btn-primary mt-3" type="submit"><i class="fa fa-database me-1"></i>{{ button_label|default('Enregistrer') }}</button>
                    {{ form_end(form) }}
                </div>
                <div class="col-md-6 text-center">
                    <i class="fa-solid fa-tasks fa-5x text-primary"></i>
                    <h2 class="mt-5">Pourquoi ajouter une sous-tâche&nbsp;?</h2>
                    <p class="lead">Ajouter une sous-tâche permet de détailler, organiser et suivre précisément les différentes étapes d'une tâche principale dans l'application.</p>
                    <h5 class="mt-4">Pourquoi associer une tâche principale&nbsp;?</h5>
                    <p class="text-muted">Associer une sous-tâche à une tâche principale facilite la gestion, le suivi et l'analyse de l'avancement global des projets.</p>
                </div>
            </div>
        </div>
    </div>

    {{ include('sub_task/_form.html.twig') }}

    <a href="{{ path('app_sub_task_index') }}">back to list</a>
{% endblock %}
