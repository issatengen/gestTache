{% extends 'base.html.twig' %}

{% block title %}New Activity{% endblock %}

{% block body %}
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Ajouter une activité</h1>
        <a href="{{ path('app_activity_index') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
            <i class="fa fa-arrow-left me-1"></i> Liste des activités
        </a>
    </div>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}" class="text-decoration-none">Tableau de bord</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_activity_index') }}" class="text-decoration-none">activités</a></li>
            <li class="breadcrumb-item active" aria-current="page">Ajouter activité</li>
        </ol>
    </nav>
    <hr class="sidebar-divider mb-4">

    <div class="container mt-5">
        <div class="card">
            <hr class="my-1">
            <div class="row container mt-2">
                <div class="col-md-6 bg-light p-4 rounded shadow">
                    {{ form_start(form) }}
                    <div>
                        <label for="libelle">Libellé de l'activité </label>
                        {{ form_widget(form.label,{'attr':{'class':'form-control'}}) }}
                    </div>
                    <div>
                        <label for="libelle">Type </label>
                        {{ form_widget(form.user,{'attr':{'class':'form-select'}}) }}
                    </div>
                    <button class="btn btn-primary mt-3" type="submit"><i class="fa fa-database me-1"></i>{{ button_label|default('Enregistrer') }}</button>
                    {{ form_end(form) }}
                </div>
                <div class="col-md-6 text-center">
                    <i class="fa-solid fa-tasks fa-5x text-primary-emphasis"></i>
                    <h2 class="mt-5">Pourquoi ajouter une activité ?</h2>
                    <p class="lead">Ajouter une activité permet de planifier, organiser et suivre les différentes tâches ou événements au sein de application.</p>
                    <h5 class="mt-4">Pourquoi sélectionner un type ?</h5>
                    <p class="text-muted">Le type d'activité permet de catégoriser chaque activité pour une meilleure gestion, un suivi précis et une analyse efficace des actions réalisées.</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
